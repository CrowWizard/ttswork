# operations-log

- 2026-04-30 17:47 +08:00：按要求将 `prisma/migrate-voice-profile.sql` 改为 PostgreSQL 9.x 兼容写法：用 `DO $$ ... IF NOT EXISTS ... $$` 替代 `CREATE TABLE/INDEX IF NOT EXISTS`，用 `UPDATE + INSERT WHERE NOT EXISTS` 替代 `ON CONFLICT`，并移除破坏性清理示例中的 `DROP COLUMN IF EXISTS`。验证：静态检索确认已无 `ON CONFLICT`、`EXCLUDED`、`DROP COLUMN IF EXISTS`；本机 `psql` 客户端版本为 13.23，但默认连接因缺少 `root` 数据库角色无法执行真实数据库回归。

- 2026-04-30 17:40 +08:00：按用户要求隐藏工作区模块导航：移除首页与 B 站视频分析页的 `WorkspaceModuleNav` 渲染；将首页 header 已登录态中的手机号、个人设置、退出登录收口为账号菜单；积分展示移到“兑换”按钮之后，改为 SVG 图标加余额数字，不再使用圆角 label 容器。验证：`npm run lint`、`npm run typecheck` 通过；首次 `npm run build` 命中 Next 增量构建产物缺失 `/_not-found`，清理 `.next` 后重跑通过，仍保留项目既有 `output: export` 与 rewrites 组合警告。

- 2026-04-30 17:28 +08:00：按 `/impeccable layout` 恢复首页工作区模块导航：在 `VoiceStudio` 主内容容器顶部接入现有 `WorkspaceModuleNav`，使首页与 `/video-analysis` 使用一致的模块切换模型；主容器改为 `flex flex-col gap-6` 并移除任务区额外 `mt-8`，让导航、标题卡与任务区节奏与视频分析页一致。验证通过：`npm run lint`、`npm run typecheck`、`npm run build`；build 仍保留项目既有 `output: export` 与 rewrites 组合警告。

- 2026-04-30 17:25 +08:00：按 `/impeccable polish` 收口首页 token 与顶部栏：将未定义的 `bg-surface-primary` 改为现有 `bg-surface` 并复用 `shadow-panel`，避免 sticky 顶部栏背景失效；登录弹窗遮罩从硬编码 `bg-black/50` 改为语义化 `bg-text-primary/50`，移除纯黑中性面残留。验证通过：检索 `components` 下已无 `bg-surface-primary` / `bg-black`，`npm run lint`、`npm run typecheck`、`npm run build` 通过；build 仍保留项目既有 `output: export` 与 rewrites 组合警告。

- 2026-04-30 17:22 +08:00：按 `/impeccable adapt` 统一首页移动端触控目标：登录弹窗关闭/取消按钮、登录模式 tab、顶部 header 的积分/使用码输入/兑换/个人设置/退出登录、录音素材删除按钮、历史语音下载入口均调整为不低于 44px 的可触控区域，保持现有低干扰 token 与组件语汇不变。验证通过：`npm run lint`、`npm run typecheck`、`npm run build`；build 仍保留项目既有 `output: export` 与 rewrites 组合警告。

- 2026-04-30 17:21 +08:00：按 `/impeccable harden` 加固首页登录与上传控件：`AuthPanel` 改为语义化 form，支持当前登录模式下按 Enter 提交短信/密码登录，并将非当前 tab 输入禁用以避免隐藏必填项参与校验；上传音频 label 增加 `focus-within` 可见焦点环，隐藏 file input 获焦时仍能定位焦点。验证：首次 `npm run typecheck` 暴露可选回调类型问题后改为可选调用，随后 `npm run lint`、`npm run typecheck` 通过；首次 `npm run build` 命中 Next 增量缓存式 `/admin/invite-codes` PageNotFoundError，未改代码重跑后通过，仍保留既有 `output: export` 与 rewrites 组合警告。

- 2026-04-30 15:34 +08:00：按 `.kilo/plans/1777532181126-mighty-forest.md` 完成语音工作台权益调整：新增 `User.pointsBalance`、`PointTransaction` 与 `POINTS` TTS 来源；注册/短信自动创建用户赠送 100 积分；使用码改为兑换 200 积分；TTS 创建改为登录用户每次扣 20 积分，Qwen 合成失败自动返还积分并记录流水。
- 2026-04-30 15:34 +08:00：前端移除首页模块导航与 TTS 试用/使用码输入；header 新增积分余额、6 位使用码兑换入口与兑换反馈；TTS 面板统一 500 字上限并按积分余额控制提交；登录弹窗文案改为积分体系。
- 2026-04-30 15:34 +08:00：录音删除改为同步作废相关纯粹版/场景版声纹并清空 active voice；前端移除单独作废声纹按钮，删除录音提示同步更新；上传与录音按钮视觉颜色已交换。
- 2026-04-30 15:34 +08:00：后台用户列表/详情增加积分余额；同步更新 `README.md`、`.env.example`、`api-server/DATABASE_DICTIONARY.md`、`api-server/init-db.sql` 的积分与使用码说明。
- 2026-04-30 15:34 +08:00：本地验证通过：根目录 `bunx prisma validate && bunx prisma generate`、API 目录 `bunx prisma validate && bunx prisma generate`、API `bunx tsc --noEmit`、API `bun run build`、API `bun test src/lib/usage-code.test.ts`、前端 `npm run typecheck`、`npm run lint`、`npm run build`、API 目录 `bunx prisma db push`。前端 build 仍保留项目既有 `output: export` 与 rewrites 组合警告。

- 2026-04-30 14:07 +08:00：按计划开始语音工作台 UI 与声纹模型重构；已同步更新前端/API Prisma schema，新增 `VoiceProfile` 并移除 `User`/`AnonymousUser` 上的 active voice 字段，后端路由改为基于 `VoiceProfile` 读取与更新 active voice。
- 2026-04-30 14:07 +08:00：将录音上传流程改为自动创建纯粹版/场景版建声任务并后台异步调用 Qwen；前端移除手动建声按钮，新增轮询 `/api/voice/profile` 的声纹生成状态提示与超时提示。
- 2026-04-30 14:07 +08:00：完成顶部 `UserHeader`、工作台文案与录音面板重构；本地执行根/API Prisma generate、`npm run lint`、`npm run typecheck`、API `bunx tsc --noEmit`、前端 `npm run build` 与 API `bun run build` 均通过。未执行 `prisma db push`，因为删除旧列属于破坏性数据库变更，需在部署前按迁移顺序先搬迁现有 active voice 数据。

- 2026-04-24 15:08 +08:00：初始化 Next.js 15 + React 19 + TypeScript + Tailwind CSS 项目骨架与基础配置。
- 2026-04-24 15:10 +08:00：建立 Prisma 数据模型，覆盖匿名用户、建声记录、TTS 任务与 active voice 约束。
- 2026-04-24 15:14 +08:00：实现 MinIO、Qwen Mock/Live、音频时长解析、匿名 Cookie 会话与接口层。
- 2026-04-24 15:18 +08:00：实现单页前端录音与 TTS 工作台，并补充 README、.env.example、docker-compose.yml。
- 2026-04-24 15:24 +08:00：修复 ESLint 配置告警与 Next typedRoutes 配置迁移，重新执行 Prisma generate、lint、typecheck、build。
- 2026-04-24 15:25 +08:00：尝试启动 docker compose 进行本地依赖联调，但当前环境未安装 docker，已记录为验证限制。
- 2026-04-24 15:32 +08:00：改为复用本机 PostgreSQL 与 MinIO；创建项目数据库角色与数据库、同步 Prisma schema，并将 README 调整为本机服务优先。
- 2026-04-24 15:34 +08:00：基于本机 PostgreSQL + MinIO 完成健康检查与端到端联调，验证建声、active voice、TTS 与下载链路可用。
- 2026-04-24 16:34 +08:00：实现录音真实时长判定、active voice 回放与作废、VoiceEnrollment 作废标记、TTS 防御性校验，并同步更新 README。
- 2026-04-24 16:42 +08:00：补强回放与作废接口约束，只允许操作当前启用声纹；修复 mock voiceId 唯一性冲突，并完成作废后阻断回放/TTS 的本机回归验证。
- 2026-04-24 16:55 +08:00：修正 Qwen 声音复刻请求结构，切换 enrollment 模型名为 `qwen-voice-enrollment`，并统一 `target_model` / TTS 模型为 `qwen3-tts-vc-2026-01-22`；本地执行 `npm run typecheck` 通过。
- 2026-04-24 17:10 +08:00：为 Qwen 建声请求补充 `preferred_name`，当前无用户名时使用 `voice-${Date.now()}` 生成时间戳命名；本地执行 `npm run typecheck` 通过。
- 2026-04-24 17:14 +08:00：为 `liveEnrollVoice` 增加建声请求与响应日志，打印请求地址、目标模型、preferred_name 及截断后的音频 data 摘要，避免日志被完整 base64 音频淹没；本地执行 `npm run typecheck` 通过。
- 2026-04-24 17:21 +08:00：新增 `.vscode/launch.json`，提供 Next.js 服务端、浏览器与全栈三套 VSCode 调试配置，便于本地断点排查接口与页面逻辑。
- 2026-04-24 17:28 +08:00：排查 VSCode 服务端断点不可用问题，确认后台已有非调试 Next 进程占用 3000 端口并停止；将 `.vscode/launch.json` 的服务端/全栈调试改为 `node` 直启 `next/dist/bin/next`，补充 `envFile`、`skipFiles` 与 `autoAttachChildProcesses`，提升 `lib/qwen.ts` 等服务端源码断点命中稳定性。
- 2026-04-24 17:30 +08:00：修复 VSCode `node` 调试参数误配问题，将 `launch.json` 中 Next.js 服务端/全栈配置的 `runtimeArgs: ["dev"]` 更正为 `args: ["dev"]`，避免 Node 将 `dev` 误识别为脚本路径导致 `MODULE_NOT_FOUND`。
- 2026-04-24 17:34 +08:00：按页面精简需求移除前端“最近建声记录”展示区块，仅调整 `components/voice-studio.tsx` 的渲染层，不改动建声数据获取与后端接口。
- 2026-04-24 17:52 +08:00：为 Qwen 建声与 TTS 请求新增服务端 `curl` 日志输出，使用 `$QWEN_API_KEY` 占位避免泄露密钥；同时在录音按钮上方增加前端实时说话时长显示，便于对照 5 秒门槛；本地执行 `npm run typecheck` 通过。
- 2026-04-24 17:58 +08:00：将录音最小时长门槛从 5 秒调整为 10 秒，前后端统一改为按“按住录音按钮的实际计时”校验并入库，不再依赖 Blob 媒体时长解析；同时将 Qwen 建声与 TTS 失败的对外提示统一收敛为“接口繁忙”；本地执行 `npm run typecheck` 通过。
- 2026-04-24 18:01 +08:00：将录音最小时长恢复为 5 秒；优化录音按钮交互，松开后立即进入“上传并建立声纹中...”禁用态，并持续到服务端返回结果，避免 `MediaRecorder.onstop` 异步间隙触发重复点击；本地执行 `npm run typecheck` 通过。
- 2026-04-24 18:03 +08:00：移除 Qwen 建声与 TTS 的 `curl` 日志输出，避免控制台噪声；同时将建声请求中的 `preferred_name` 固定为 `guanyu`；本地执行 `npm run typecheck` 通过。
- 2026-04-24 18:10 +08:00：新增音频 MIME 白名单，仅允许 `audio/wav`、`audio/mpeg`、`audio/mp4`；前端录音仅尝试这三种 MediaRecorder 输出，后端与 Qwen 建声侧同步校验并按对应扩展名入库，确保上传内容符合 `data:<mediatype>;base64,<data>` 格式约束；本地执行 `npm run typecheck` 通过。
- 2026-04-24 18:16 +08:00：前端建声区新增 MP3 文件上传入口，选中文件后先读取音频时长并复用现有建声接口提交；上传期间按钮保持禁用并显示“上传并建立声纹中...”，页面文案同步更新为支持录音与 MP3 上传；本地执行 `npm run typecheck` 通过。
- 2026-04-24 18:18 +08:00：按需求撤回 MP3 文件上传入口，前端建声重新收敛为录音流程；录音 MIME 改为仅接受 `audio/mpeg`，从页面侧直接生成并上传 MP3，若浏览器不支持 `MediaRecorder(audio/mpeg)` 则直接提示更换浏览器；本地执行 `npm run typecheck` 通过。
- 2026-04-24 18:24 +08:00：按需求恢复前端录音的兼容优先策略，优先使用 `audio/webm;codecs=opus` / `audio/webm` / `audio/mp4` 录制；录音结束后在浏览器端解码并转成 `audio/wav` 再上传，确保服务端收到的建声文件统一为 WAV，同时不再强制浏览器原生录制 MP3；本地执行 `npm run typecheck` 通过。
- 2026-04-24 18:48 +08:00：修复 Qwen TTS 播放链路，识别到接口当前返回 `application/json` 包裹的音频元数据而非直接音频字节；改为在服务端先解析 JSON，再从 `output.audio.url` 或 `output.audio.data` 拉取/解码真实音频并按实际 `content-type` 入库，避免前端 `<audio>` 读取到 JSON 假文件导致无法播放；本地执行 `npm run typecheck` 通过。
- 2026-04-24 19:30 +08:00：实现前后端分离部署架构。API 层从 Next.js Route Handlers 提取为独立 Hono + Bun 应用（`api-server/`），编译为单可执行文件 `voice-mvp-api`（96MB）；前端配置 `output: "export"` 导出纯静态文件（852KB），可由 Nginx/CDN 托管。移除 Next.js 项目中的 `app/api/` 目录（API 已迁至 `api-server/`）。附赠 Nginx 反代配置示例。
- 2026-04-24 19:35 +08:00：添加 Makefile（dev/build/install/clean 一键操作）、更新 .vscode 调试配置适配前后端分离（含 Bun API 调试 + 全栈联合调试）、创建 systemd service 文件和部署脚本 deploy.sh。API 全量配置外部化：支持 YAML 配置文件（`config.yaml`）+ 环境变量覆盖，搜索路径为 `CONFIG_PATH` 环境变量 → `./config.yaml` → `./config.yml` → `/etc/voice-mvp/config.yaml`，覆盖端口/数据库/MinIO/Qwen/Cookie/日志目录。确认 Rocky 9 编译产物依赖 GLIBC 2.25，CentOS 7（GLIBC 2.17）需在目标机器上重新编译。
- 2026-04-25 02:05 +08:00：针对“录音转码后仍提示仅支持 WAV/MP3/M4A”问题，在前端录音停止与提交建声前补充音频 name/type/size/chunkCount 诊断日志，并在 API 收包后记录 multipart 中的实际 MIME、文件名与大小，用于区分“前端静态资源未更新”与“multipart MIME 丢失/回退为 application/octet-stream”两类根因；暂未修改业务逻辑。
- 2026-04-25 02:10 +08:00：根据线上日志确认建声上传实际 MIME 为 `audio/x-wav`，属于 `audio/wav` 的常见别名；在前后端音频 MIME 识别中新增别名规范化，将 `audio/x-wav` 统一映射为 `audio/wav`，并让 API 入库/Qwen 建声请求都使用规范化后的 MIME，消除误判拦截；本地执行前端与 API TypeScript 校验通过。
- 2026-04-25 11:24 +08:00：升级前端与 API 的 Prisma 依赖到 7.8.0，按 Prisma 7 要求迁移 datasource URL 到 `prisma.config.ts` 并引入 PostgreSQL adapter；为 TTS 生成结果与历史语音新增下载入口；修复手机端工作台头部与下方模块宽度/内边距不一致；本地执行 Prisma generate、前端/API typecheck 与 build 通过，Node 20 环境安装 Prisma 7 时存在 `@prisma/streams-local` 建议 Node >=22 的 engine 警告。
- 2026-04-25 11:32 +08:00：修复部署与文档中的旧 Prisma 命令，移除 `prisma@6` 与 `npx` 用法，统一改为 `bunx prisma`；`deploy.sh` 部署时同步复制 API 的 Prisma 7 配置与依赖清单，并在安装目录使用 Bun 安装依赖后执行 `bunx prisma db push`；新增 `api-server/update.sh` 自动更新脚本，覆盖拉取代码、安装依赖、生成 Prisma Client、构建前端/API、部署与重启服务；本地完成脚本语法检查、旧命令残留检索、根项目/API 的 `bunx prisma generate` 与 typecheck 验证。
- 2026-04-25 11:36 +08:00：调整自动更新发布阶段：前端构建后直接发布到 `/var/www/voice-mvp`；API 更新不再调用完整 `deploy.sh` 覆盖部署流程，而是先将 `/opt/voice-mvp` 备份到 `/opt/voice-mvp-backups/voice-mvp-<timestamp>`，再复制最新二进制、Prisma 配置与依赖清单到 `/opt/voice-mvp`，最后调用 `deploy.sh --db-only` 仅同步数据库并重启服务；本地执行脚本语法检查通过。
- 2026-04-25 11:40 +08:00：继续精简自动更新脚本：`/opt/voice-mvp` 不再接收 `package.json` 与 `package-lock.json`，数据库同步改为在 API 源码目录用 `SOURCE_DIR` 执行 `bunx prisma db push`；合并“构建 API 单文件”和“更新 API 文件”为同一步，整体步骤从 8 步收敛为 7 步；删除新生成的 `api-server/package-lock.json` 并加入 `.gitignore`，保留根目录 `bun.lock` 作为 Bun 统一依赖锁文件；本地执行脚本语法与残留检索通过。
- 2026-04-25 11:59 +08:00：修复服务器执行 `update.sh` 时 `bun install --frozen-lockfile` 因 lockfile 需要更新而失败的问题；自动更新脚本默认使用普通 `bun install` 允许服务器刷新 `bun.lock`，如需严格锁文件校验可设置 `STRICT_LOCKFILE=true` 后再运行；本地执行脚本语法检查通过。
- 2026-04-25 12:01 +08:00：为避免服务器运行 `update.sh` 后本地生成/更新 `bun.lock` 导致下次 `git pull --ff-only` 需要合并，已将 `bun.lock` 加入 `.gitignore` 并从 Git 索引移除，后续不再提交该锁文件。
- 2026-04-25 12:04 +08:00：继续清理 Git 中残留的 Bun 锁文件，将 `**/bun.lock` 加入 `.gitignore`，并从 Git 索引移除 `api-server/bun.lock`，避免 API 子目录的 Bun lock 继续出现在远程仓库。
- 2026-04-25 12:12 +08:00：修复服务器执行 Prisma 命令时 `/opt/ttswork/prisma.config.ts` 因缺少 `DATABASE_URL` 加载失败的问题；根项目与 API 的 `prisma.config.ts` 现在优先使用环境变量，其次从 `CONFIG_PATH`、当前目录、`api-server/config.yaml`、`/opt/voice-mvp/config.yaml`、`/etc/voice-mvp/config.yaml` 读取 `database` 段并组合 PostgreSQL 连接串；为避免根项目额外依赖，配置文件使用轻量 YAML database 段解析；本地执行根项目/API 的 `bunx prisma generate` 与 typecheck 通过。
- 2026-04-25 12:14 +08:00：修复 `update.sh` 在 `set -u` 下执行空数组展开导致 `BUN_INSTALL_ARGS[@]: unbound variable` 的问题，改为 `run_bun_install` 函数按 `STRICT_LOCKFILE=true` 显式选择是否追加 `--frozen-lockfile`；本地执行脚本语法检查通过。
- 2026-04-25 12:16 +08:00：进一步消除 `update.sh` 中可能触发旧 Bash/子 shell 行为差异的安装封装函数，改为在前端和 API 依赖安装处直接用 `if [ "${STRICT_LOCKFILE:-false}" = "true" ]` 内联选择 `bun install` 或 `bun install --frozen-lockfile`；本地确认脚本中已无 `BUN_INSTALL_ARGS` 与 `run_bun_install` 残留，语法检查通过。
- 2026-04-25 12:26 +08:00：修复 Prisma 7 执行 `db push` 时不再支持 `--skip-generate` 导致的 `unknown or unexpected option` 报错；部署脚本数据库同步命令与手动提示均改为 `bunx prisma db push --accept-data-loss`；本地执行脚本语法检查并用 `bunx prisma db push --help` 确认参数兼容。
- 2026-04-25 13:54 +08:00：按手机号账号方案重构后端基础设施：Prisma 主体从 `AnonymousUser` 切换为 `User + Session + SmsVerification`，新增会话 Cookie、密码哈希、阿里云短信 `mock/live` 双实现与 `/api/auth/*` 路由，并将语音/TTS 业务接口统一改为显式登录态校验；同步更新 `.env.example`、`api-server/config.example.yaml` 与 README 的认证配置说明。
- 2026-04-25 13:59 +08:00：为 `.env.example` 中新增的会话 Cookie 与阿里云短信配置逐项补充中文注释，说明默认值含义、本地/生产差异与真实密钥注意事项。
- 2026-04-25 14:05 +08:00：为 Next.js 开发环境新增 `/api/:path*` rewrite，将前端开发服务器的 API 请求转发到 `http://127.0.0.1:3001/api/:path*`，并在生产构建中保持空 rewrite 以兼容静态导出部署。
- 2026-04-25 14:09 +08:00：更新 VS Code 调试配置：API 调试改为读取根目录 `.env` 并通过 Bun inspector 运行 `api-server/src/index.ts`，全栈调试改为 compound 同时启动 API 与 Next dev，浏览器入口统一使用 `http://127.0.0.1:3000`。
- 2026-04-25 14:17 +08:00：简化未登录认证页面：默认只展示短信登录，移除密码登录 tab 与右侧能力介绍，在登录表单底部提供“免费注册”切换入口；注册成功继续沿用后端自动创建会话，前端直接进入已登录工作台。
- 2026-04-25 14:21 +08:00：按“短信登录即注册”规则调整认证链路：`/api/auth/sms/send` 的登录场景允许未注册手机号发送验证码，`/api/auth/login/sms` 在验证码通过后自动 `upsert` 用户并创建会话；前端未登录页只保留单一“登录/短信登录”表单，不再展示注册入口。
- 2026-04-25 14:26 +08:00：根据阿里云短信控制台信息更新示例配置：签名为“速通互联验证码”，模板 Code 为 `100001`，模板变量仍使用 `{"code":"##code##","min":"5"}`，登录/注册短信方案名统一为“登录/注册模板”。
- 2026-04-25 14:48 +08:00：针对 Prisma `db push` 添加外键时报 `VoiceEnrollment_userId_fkey` 失败的问题，将 `api-server/prisma/schema.prisma` 切换为 `relationMode = "prisma"`，保留 Prisma 关系查询能力但禁止数据库层创建外键；同步更新 `api-server/drop-fk.sql`，覆盖当前 `User`、`Session`、`VoiceEnrollment`、`TtsJob` 的全部外键清理命令。
- 2026-04-25 14:50 +08:00：修复 `relationMode = "prisma"` 下 Prisma 校验不接受 `NoAction` 的问题，将关系字段的引用动作统一调整为 `Restrict` / `Cascade` 组合，并为 `TtsJob.voiceEnrollmentId` 显式补充索引，避免失去数据库外键隐式索引后查询退化。
- 2026-04-25 14:53 +08:00：定位到服务器是在仓库根目录执行 `bun run prisma:push`，实际读取的是根目录 `prisma/schema.prisma` 而非 `api-server/prisma/schema.prisma`；已将根目录 schema 同步切换为 `relationMode = "prisma"` 并调整引用动作与索引，避免根目录脚本继续向数据库补建外键。
- 2026-04-25 14:58 +08:00：修复 `api-server` 对仓库根 `.env` 读取不稳定的问题：为运行时 `src/lib/config.ts` 与 `api-server/prisma.config.ts` 显式补充 `../.env` 与 `api-server/.env` 的加载逻辑，优先级为外部环境变量 > `api-server/.env` > 根目录 `.env`；同时增加脱敏配置来源日志，便于定位当前工作目录与实际命中的配置文件。
- 2026-04-25 16:23 +08:00：实现首页认证与个人设置改造计划（方案A）：
   - 扩展 Prisma SmsScene 枚举，新增 PASSWORD_CHANGE 场景
   - 扩展 SMS 模块与配置，支持 password_change 短信场景
   - 新增密码设置/修改的 validation schema
   - 新增 /api/auth/password/set 与 /api/auth/password/change 接口
   - 扩展 /api/auth/sms/send 端点，支持 password_change 场景并校验登录态
   - 重构 VoiceStudio 组件：移除整页阻塞 authLoading，改为后台检查+局部切换；首页同时支持短信登录与用户名密码登录；添加注册链接
   - 新增独立注册页 app/register/page.tsx，支持手机号+验证码+可选密码注册
   - 新增个人设置页 app/settings/page.tsx，支持无密码账号设置密码、已设密码账号短信校验后修改密码
   - 首页右上角账号区域增加“个人设置”入口
   - 所有变更保持静态导出部署兼容，lint/typecheck/build 通过
- 2026-04-25 17:22 +08:00：依据 `.kilo/plans/1777108306467-clever-river.md` 完成前端静态代码审计，未改动业务代码；新增 `.kilo/reports/1777108306467-clever-river-audit.md`，覆盖 Accessibility、Performance、Responsive Design、Theming、Anti-Patterns 五维评分、分级问题与推荐执行命令。
- 2026-04-25 17:38 +08:00：按 `/impeccable teach` 要求新增 `PRODUCT.md`，以“MVP 约束型”方式固化当前已验证产品边界，覆盖文档目的、产品定位、一句话定位、可验证能力、目标用户与使用前提、核心任务流、页面职责、设计原则、文案语气、非目标、待确认清单及对后续 `DESIGN.md` / UI 的直接约束；同时在日志中登记本次文档操作，未执行命令验证。
- 2026-04-25 17:46 +08:00：修复 `/impeccable teach` 后续 QA 发现的 PRODUCT 文档事实边界问题：补充短信登录自动创建账号、密码登录、active voice 管理、TTS 历史与下载、设置页账号信息等已验证能力；将历史记录与下载从待确认项移出，收敛真正未知项；补充 README 历史“单页”表述与当前多页面实现的跨文档一致性说明；同步整理本日志中新近条目的时间顺序。
- 2026-04-25 17:47 +08:00：按 `.kilo/plans/1777108441075-crisp-river.md` 完成前端最小可用修复：在 `tailwind.config.ts` 与 `app/globals.css` 新增语义化主题 token、统一 `focus-visible` 与表单/按钮基础样式；新增 `components/ui/status-message.tsx` 并替换首页、注册页、设置页的重复消息块；重构 `components/voice-studio.tsx`，补齐录音按钮键盘可达性，拆分 `workspaceError` / `workspaceNotice` 状态语义，将录音计时降频到 250ms，并收口首页头部、active voice、空态与移动端展示；同步优化 `app/register/page.tsx`、`app/settings/page.tsx` 的响应式承载与加载态，更新 `app/layout.tsx` 基础元信息；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 17:51 +08:00：继续执行 `/impeccable document`，核对 `DESIGN.md`、`DESIGN.json`、`PRODUCT.md` 与当前前端实现的一致性；修正 `DESIGN.json` 中录音控件示例的最小时长为 5 秒，使其与 `lib/constants.ts` 和 `PRODUCT.md` 保持一致。
- 2026-04-25 17:52 +08:00：按 `/impeccable harden` 继续加固前端可访问性：录音触屏事件显式阻止合成鼠标事件，键盘长按不再重复切换录音；TTS 完成结果、通用状态消息与设置页加载态补齐 `role`、`aria-live`、`aria-atomic` 等辅助技术语义，并确认页面组件无局部弱化焦点的 `outline-none`；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 17:56 +08:00：按 `/impeccable colorize` 重建前端色彩系统：将 Tailwind 颜色从浅色原始 token 扩展为 `canvas`、`surface`、`border`、`text`、`action`、`status` 语义 token；全局背景改为暖灰到冷蓝的径向底色，表单、按钮、状态消息与工作台/注册/设置页面迁移到新 token，移除主要页面中的 `slate`、`amber`、`emerald`、`rose`、`white` 硬编码色；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 18:00 +08:00：按 `/impeccable optimize` 优化首页录音渲染路径：将录音 elapsed 展示下沉到 `RecordingElapsedStatus` 局部组件，根组件仅保留开始时间与最终时长计算，避免录音计时 interval 推动 `VoiceStudio`、头部与 TTS 区整体重渲染；展示刷新频率收敛为 500ms，上传校验仍使用停止瞬间的精确时长；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 18:04 +08:00：按 `/impeccable layout` 补强移动端布局：active voice 区域保留完整换行与 title 查看能力，建声标题徽标允许换行，作废按钮明确保持 44px 触控目标；设置页账号信息行补充 `min-w-0`、标签不压缩与值右对齐/换行策略，降低窄屏和大字号下的挤压风险；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 18:04 +08:00：按 `/impeccable clarify` 统一前端状态反馈层级：`StatusMessage` 增加标题/正文结构与默认语义标题，首页、注册页、设置页的成功、错误、调试验证码提示改为一致的信息层级；建声、作废与 TTS 成功反馈继续与错误反馈分离，避免成功消息复用错误语义；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 18:06 +08:00：按 `/impeccable quieter` 收敛前端视觉噪声：全局背景从径向渐变改为纯 canvas，卡片/面板/输入/按钮/状态消息圆角下调，阴影弱化为 1px 级边界提示，并移除分段控件的额外阴影；同步调整首页录音、TTS 结果、历史语音与设置页加载骨架的圆角，降低模板化白卡、柔阴影与大圆角重复感；`npm run lint` 首次触发本地段错误，改用 `./node_modules/.bin/eslint .` 通过，`npm run typecheck` 与 `npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 18:10 +08:00：按 `/impeccable polish` 完成前端最终收口：首页建声区增加三步任务提示，TTS 区补充 active voice 就绪说明、字符计数、输入 placeholder 与结构化历史空态；注册页增加自动登录与密码可后补说明；设置页优化加载骨架和安全提示卡片；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 18:13 +08:00：重新执行 `/impeccable audit` 静态审计；确认前序 teach/document/harden/colorize/optimize/layout/clarify/quieter/polish 已修复旧报告中录音键盘可达性、状态朗读、焦点样式、主题 token、移动端触点与模板化视觉等主要问题；更新 `.kilo/reports/1777108306467-clever-river-audit.md`，当前评分提升为 17/20，剩余问题聚焦 `VoiceStudio` 文件边界、`DESIGN.md` token 漂移与登录模式切换语义；本次仅更新报告，未运行构建命令。
- 2026-04-25 18:16 +08:00：按 `/impeccable document` 同步 `DESIGN.md` 与当前 Tailwind token，将旧暖白/墨黑/24px/重阴影设计快照更新为 `canvas`、`surface`、`action-*`、状态边框、16px/18px 圆角与 1px 级阴影体系；本次仅更新设计文档，未改动业务代码。
- 2026-04-25 18:21 +08:00：按 `/impeccable optimize` 将 `VoiceStudio` 从单文件内部拆分推进到文件边界与状态 hook 边界；保留 `components/voice-studio.tsx` 入口，新增 `components/voice-studio/` 下的认证、头部、建声、TTS 面板、共享类型/工具与 `useVoiceStudioState`，不改变页面导入路径和交互行为；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 18:22 +08:00：按 `/impeccable harden` 为首页登录模式切换补齐 `tablist` / `tab` / `tabpanel` 语义、`aria-selected`、`aria-controls` 与方向键/Home/End 键盘切换；同步在审计报告中补充键盘焦点顺序复核；本地执行 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-25 22:14 +08:00：修复首页登录框短信验证码行样式，移除验证码输入框继承的额外上边距，统一输入框与“发送验证码”按钮高度，并缩短验证码输入框宽度；本地执行 `npm run lint`（仅既有 warning）与 `npm run typecheck` 通过。
- 2026-04-25 22:18 +08:00：继续修复首页登录框模式切换抖动问题，统一短信登录与密码登录面板最小高度、标签到输入行间距、输入框高度和登录按钮位置；小屏下验证码输入框改为 `w-36` 并让发送按钮不换行，避免横向挤压；本地执行 `npm run lint`（仅既有 warning）与 `npm run typecheck` 通过。
- 2026-04-25 22:20 +08:00：将首页密码登录输入框的锁图标路径调整为验证码输入框同款 SVG，减少登录模式切换时的视觉差异；本地执行 `npm run lint`（仅既有 warning）与 `npm run typecheck` 通过。
- 2026-04-25 22:24 +08:00：修正首页登录框验证码/密码输入行图标垂直位置，将图标从全局 `top-[40%]` 覆盖为输入行内 `top-1/2 -translate-y-1/2` 居中，确保与可输入文字高度一致；本地执行 `npm run lint`（仅既有 warning）与 `npm run typecheck` 通过。
- 2026-04-25 22:27 +08:00：按视觉复核结果撤回锁图标继续上移的临时调整，验证码/密码锁图标保持 `top-1/2 -translate-y-1/2` 居中，电话图标不变；本地执行 `npm run typecheck` 通过，`npm run lint` 本次运行超时，前一轮已通过且仅有既有 warning。
- 2026-04-25 22:29 +08:00：调整首页验证码登录输入行宽度，验证码输入框与“发送验证码”按钮外层设为 `w-full`，输入框占剩余空间、按钮不压缩，使组合总宽度与手机号输入框一致；本地执行 `npm run typecheck` 通过。
- 2026-04-25 22:34 +08:00：禁用首页建声录音按钮的 `Space` / `Enter` 键盘触发，保留拦截默认行为但不再通过键盘开始或结束录音，并同步更新建声提示文案；调整个人设置与退出登录按钮为固定高度、水平垂直居中；本地执行 `npm run typecheck` 与 `npm run lint` 通过，lint 仅剩既有 warning。
- 2026-04-25 22:35 +08:00：按要求删除建声区“建声过程中不响应 `Space` 或 `Enter` 键盘触发”提示文案，保留键盘触发禁用行为不变；本地执行 `npm run typecheck` 通过。
- 2026-04-25 22:52 +08:00：按 TTS 面板文案补齐匿名 30 字内免费生成一次链路：未登录 `POST /api/tts` 可使用 `QWEN_TRIAL_VOICE_ID` 返回 data URL 音频，前端区分匿名免费次数与登录 active voice 使用；登录用户不再受匿名 30 字/一次限制。根项目 `npm run typecheck`、`npm run lint`、`npm run build` 通过，lint/build 仅保留既有 hooks warning 与静态导出 rewrites warning；API `bunx tsc --noEmit` 仍受既有 Prisma relation 类型问题阻断。
- 2026-04-25 23:14 +08:00：实现匿名 Cookie 记录持久化与登录迁移：新增 `AnonymousUser`，`VoiceEnrollment` / `TtsJob` 支持登录用户或匿名用户归属；匿名建声、active voice 回放/作废、TTS 历史与下载均按匿名 Cookie 可见；短信/密码/注册登录成功后迁移匿名建声与 TTS，若登录用户没有 active voice 则采用匿名 active voice，已有 active voice 时不替换。同步修复 API 会话查询不依赖 Prisma relation include；执行根/API `bunx prisma generate`、API `bunx prisma db push`、根 `npm run typecheck`、API `bunx tsc --noEmit`、根 `npm run lint`、根 `npm run build`、API `bun run build` 通过，lint/build 仅保留既有 hooks warning 与静态导出 rewrites warning。
- 2026-04-26 00:16 +08:00：按方案 A 修复 `AnonymousUser` 历史数据导致的 Prisma `db push` 失败：在 `api-server/deploy.sh` 增加旧表兼容性回填，先为缺失的 `tokenHash`、`expiresAt`、`lastSeenAt` 补列并为存量匿名记录生成一次性占位值，再执行 `bunx prisma db push --accept-data-loss`；同步更新 `api-server/init-db.sql`，保证新初始化库直接包含 `AnonymousUser` 所需字段与索引；本地执行部署脚本语法检查与 Prisma CLI 参数检查通过。
- 2026-04-26 00:28 +08:00：修正 `api-server/deploy.sh` 的 `AnonymousUser` 兼容性回填顺序：考虑到 Prisma `db execute` 在单次 SQL 脚本中执行 `ALTER TABLE` 后紧接着 `UPDATE` 新列时，服务器侧仍可能报列不存在，现改为“补列 → 回填历史数据 → 建索引”三次独立执行，并在任一步失败时立即中止，避免回填未生效仍继续执行 `prisma db push` 造成误导。
- 2026-04-26 00:34 +08:00：继续兼容服务器侧 `AnonymousUser` 补列失败场景：将 `deploy.sh` 中的 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 改为 `DO $$ ... information_schema.columns ... ALTER TABLE ... $$` 条件补列，避免部分环境/执行器对该写法的兼容差异；补列失败提示同步增加 schema 指向检查。
- 2026-04-26 00:39 +08:00：优化首页建声区步骤提示的移动端呈现：`components/voice-studio/recording-panel.tsx` 在手机端将原先每张卡片重复展示的 `Step 1/2/3` 标签收敛为顶部单一 `Steps` 标签，桌面端继续保留分步标签，减少窄屏视觉噪声并保持步骤信息完整。
- 2026-04-26 00:41 +08:00：按反馈继续调整首页建声区步骤提示：撤回“移动端仅保留单一 `Steps` 标签”的方案，改为手机端将三个 step 内容合并进一个圆角容器，并通过分隔线区分步骤；桌面端仍保留三张独立步骤卡片，兼顾窄屏整洁度与大屏扫描效率。
- 2026-04-26 00:57 +08:00：为“前端只显示短信发送失败”补充后端最小诊断日志：`api-server/src/lib/sms.ts` 现在在短信发送开始、配置缺失、阿里云返回体与异常分支输出脱敏日志，`api-server/src/routes/auth.ts` 在 `/api/auth/sms/send` 失败时追加 scene/mockMode/脱敏手机号，便于通过 journal 直接区分是 `sms.mockMode`、配置缺失还是阿里云业务错误码导致。
- 2026-04-26 01:03 +08:00：按排障结论继续为短信 SDK 构造器兼容问题补充最小诊断：在 `api-server/src/lib/sms.ts` 的 `createSmsClient()` 中输出 `OpenApi.Config`、`OpenApi.default`、`OpenApi.default.Config`、`Dypnsapi` 的运行时类型与 `@alicloud/openapi-client` 导出键名，便于复现 `Object is not a constructor` 时直接从 journal 判断是哪个构造器在 Bun 编译产物中失真。
- 2026-04-26 01:08 +08:00：根据服务器日志确认 `OpenApi.Config` 正常但 `Dypnsapi` 在 Bun 编译产物中退化为对象包装，已在 `api-server/src/lib/sms.ts` 为短信客户端初始化增加最小兜底：优先直接使用 `Dypnsapi` 构造器，若其不是函数则回退到 `Dypnsapi.default`，两者都不可用时抛出显式初始化错误；诊断日志同步补充 `Dypnsapi.default` 的运行时类型。
- 2026-04-25 23:18 +08:00：修复声纹回放接口过度限制问题：`/api/voice/enrollments/:id/audio` 改为校验当前登录/匿名归属、READY 状态与未作废即可回放，不再额外要求该记录必须等于当前 active voice，避免匿名迁移或 active voice 切换后已归属声纹无法回放；本地执行 API `bunx tsc --noEmit`、根 `npm run typecheck`、API `bun run build`、根 `npm run lint` 通过，lint 仅保留既有 hooks warning。
- 2026-04-25 23:29 +08:00：继续修复匿名声纹回放身份不稳定问题：匿名访问 `/api/voice/profile` 与 `/api/tts` 历史时改为主动创建匿名 Cookie，避免首次匿名建声只依赖建声 POST 响应写 Cookie，导致后续 profile/audio 读取不到同一匿名身份；本地执行 API `bunx tsc --noEmit`、根 `npm run typecheck`、API `bun run build`、根 `npm run lint` 通过，lint 仅保留既有 hooks warning。
- 2026-04-25 23:36 +08:00：修复匿名声纹前端可见但无法播放的问题：active voice 回放不再直接把受保护接口 URL 填给 `<audio>`，改为前端先用 `fetch(..., credentials: "include")` 拉取录音 Blob，再生成 `blob:` URL 供 `<audio>` 播放，确保匿名 Cookie 随请求发送；回放加载期间显示“正在准备录音回放”。本地执行 `npm run typecheck` 与 `npm run build` 通过，build 仅保留既有 hooks warning 与静态导出 rewrites warning。
- 2026-04-25 23:41 +08:00：修复刷新时声纹回放控件一闪后被覆盖的问题：前端不再在 Blob 回放加载开始/失败时清空已有 `activeVoicePlaybackUrl`，并让 `<audio>` 优先使用 Blob URL、Blob 未就绪时回退到接口 `playbackUrl`，避免 profile 重新拉取或 Blob 加载竞争导致回放区消失；本地执行 `npm run typecheck`、`npm run lint`、`npm run build` 通过，lint/build 仅保留既有 hooks warning 与静态导出 rewrites warning。
- 2026-04-25 23:46 +08:00：修复手机端声纹回放与建声/TTS 模块变宽问题：根页面与工作台容器增加 `min-w-0`、`overflow-x-hidden`，grid 列改为 `minmax(0, ...)`，`app-card` / `app-panel` 强制允许收缩；所有音频控件增加 `w-full max-w-full` 并包裹 `overflow-hidden` 容器，避免移动端原生 `<audio>` 最小宽度撑开卡片。执行 `npm run typecheck`、`npm run lint` 通过；首次 `npm run build` 命中 Next 增量缓存 PageNotFoundError，清理 `.next` 后重跑通过，仍仅保留既有 hooks warning 与静态导出 rewrites warning。
- 2026-04-25 23:47 +08:00：调整当前声纹回放区：新增独立播放进度文本，显示为 `0.0s / 3.5s` 形式并随 `<audio>` 播放进度更新；将作废当前声纹的 `×` 按钮移动到回放标题与时长同一行右侧，播放器独占下一行以改善手机端可用宽度。本地执行 `npm run typecheck`、`npm run lint`、`npm run build` 通过，lint/build 仅保留既有 hooks warning 与静态导出 rewrites warning。
- 2026-04-26 11:58 +08:00：将“建声录音”改为“两步式 + 双声纹”链路：后端新增 `VoiceRecording` 与 `VoiceProfileKind`，录音先上传至 MinIO 并保存公网 URL，再分别建立纯粹版/场景版声纹；TTS 新增场景列表接口、`instruction` 透传、历史类型标记，并将 CosyVoice 调用切换到 `cosyvoice-v3.5-plus` 非流式接口。前端同步拆分为“上传录音 -> 建立纯粹版/场景版声纹 -> 选择场景生成 TTS”，同时在 `api-server/nginx.conf` 增加 `/minio/` 反向代理以对外暴露 MinIO 资源。
- 2026-04-26 12:18 +08:00：修复两步建声上线时 Prisma `db push` 被历史数据阻断的问题：在 `api-server/deploy.sh` 增加“旧 `VoiceEnrollment`/`TtsJob` -> 新 `VoiceRecording` + `profileKind` + `recordingId`”兼容性补列与回填流程，将存量建声记录一对一迁入 `VoiceRecording`、为旧建声/旧 TTS 统一回填 `PURE` 类型，并把旧 `activeVoiceEnrollmentId` 迁移到新的纯粹版 active 字段；同步重写 `api-server/init-db.sql` 与 README 数据模型摘要，避免新部署继续落到旧结构。
- 2026-04-26 12:21 +08:00：按开发环境部署脚本不可用的反馈，新增 `api-server/backfill-two-step-voice.sql` 手工回填脚本，覆盖两步建声升级所需的枚举、补列、旧 `VoiceEnrollment` 到 `VoiceRecording` 的一对一迁移、`profileKind/recordingId` 回填以及旧 `activeVoiceEnrollmentId` 到纯粹版 active 字段迁移，供执行 `prisma db push` 前直接落库使用。
- 2026-04-26 12:28 +08:00：按“MinIO 外网 baseUrl 不入库”的要求，移除 `VoiceRecording.publicUrl` 字段与相关落库逻辑；建声时改为使用运行时配置 `minio.publicBaseUrl` + `bucket/objectKey` 动态拼接公网录音地址，仅在数据库保留 `bucket`、`objectKey` 与 `minioUri`。同步更新 Prisma schema、录音/建声路由、初始化 SQL、部署兼容 SQL 与手工回填 SQL；本地执行 API `bunx prisma generate`、`bunx tsc --noEmit`、`bash -n api-server/deploy.sh` 通过。
- 2026-04-26 12:31 +08:00：新增 `api-server/DATABASE_DICTIONARY.md` 数据字典文档，基于当前 Prisma schema 梳理所有枚举、表结构、字段中文释义、索引与关系说明，便于后续开发、联调和迁移排查时统一理解数据库语义。
- 2026-04-26 12:49 +08:00：调整录音素材与声纹工作流：后端 `voice profile` 仅返回最新 3 份录音素材，并新增删除录音素材接口；删除时同步删除 MinIO 对象与 `VoiceRecording` 记录。前端录音素材列表新增回放与删除按钮，删除后自动校正当前选中素材；建声继续严格基于当前选中的录音素材发起。同步移除声纹回放展示，当前声纹区仅保留状态、声纹 ID 与作废操作。本地执行 API `bunx tsc --noEmit`、根 `npm run typecheck`、根 `npm run lint` 通过。
- 2026-04-26 12:56 +08:00：修正录音素材与 TTS 历史回放控件显示问题：去掉列表内 `<audio>` 的固定 `h-8` 高度，恢复浏览器原生控件完整可见；同时将录音素材删除按钮改为整列垂直居中显示，避免在素材卡片顶部偏移。执行 `npm run typecheck` 通过。
- 2026-04-26 16:17 +08:00：完善后端统一日志系统：新增 `src/lib/logger.ts`，按 `server.logLevel` 同时输出结构化 JSON 日志到控制台与 `${logDir}/api-server.log`；在 `index.ts` 增加请求级中间件与全局异常处理，所有返回 `>=400` 的前端请求都会记录方法、路径、状态码、耗时、请求 ID 与错误响应体。建声/TTS 外部接口失败不再统一返回“接口繁忙”，改为透传可定位错误信息，并在 `debug` 级别额外返回调试上下文；同时将认证与短信模块的零散 `console` 输出收口到统一 logger。执行 API `bunx tsc --noEmit`、API `bun run build`、根 `npm run typecheck` 通过。
- 2026-04-26 16:27 +08:00：按纯粹版/场景版建声应分流的要求，重构 `api-server/src/lib/qwen.ts`：新增 `enrollPureVoice` 与 `enrollSceneVoice`，其中纯粹版恢复为旧的“直接上传音频内容”建声方式（`qwen-voice-enrollment` + `qwen3-tts-vc-2026-01-22`），场景版继续使用新的“录音公网 URL + prefix + cosyvoice-v3.5-plus”方式；`api-server/src/routes/voice-enrollments.ts` 改为按 `profileKind` 显式分流，并补充建声模式日志。执行 API `bunx tsc --noEmit`、API `bun run build`、根 `npm run typecheck` 通过。
- 2026-04-26 16:35 +08:00：将文本转语音链路也按纯粹版/场景版显式分流：`api-server/src/lib/qwen.ts` 新增 `synthesizePureSpeech` 与 `synthesizeSceneSpeech`，纯粹版恢复为旧模型 `qwen3-tts-vc-2026-01-22` 的合成方式（不携带 `instruction`），场景版继续使用 `cosyvoice-v3.5-plus` 与场景提示词合成；`api-server/src/routes/tts.ts` 改为按 `job.profileKind` 调用对应实现，并增加合成模式日志字段，确保建声模型与 TTS 模型链路一致。执行 API `bunx tsc --noEmit`、API `bun run build`、根 `npm run typecheck` 通过。
- 2026-04-26 16:42 +08:00：清理根目录 `lib/` 下已失效的服务端遗留文件，删除 `lib/http.ts`、`lib/minio.ts`、`lib/prisma.ts`、`lib/qwen.ts`、`lib/validation.ts`；保留前端仍在使用的 `audio-browser.ts`、`audio-format.ts`、`audio.ts`、`constants.ts`。同步修正 README 中对 Qwen 封装位置和关键目录的旧描述。执行根 `npm run typecheck`、API `bunx tsc --noEmit` 通过。
- 2026-04-26 16:58 +08:00：修复正式环境 `prisma db push` 因旧 `activeVoiceEnrollmentId` 唯一约束阻塞的问题：在 `api-server/backfill-two-step-voice.sql` 与 `api-server/deploy.sh` 的兼容回填阶段，先显式删除 `User_activeVoiceEnrollmentId_key` 与 `AnonymousUser_activeVoiceEnrollmentId_key` 旧约束，再交给 Prisma 删除旧列，避免 PostgreSQL 因“约束依赖底层索引”拒绝直接 `DROP INDEX`。执行 `bash -n api-server/deploy.sh`、API `bunx tsc --noEmit` 通过。
- 2026-04-26 17:29 +08:00：将 Qwen 配置层按纯粹版/场景版完全拆分：`api-server/src/lib/config.ts` 新增 `qwen.pureEnrollUrl`、`qwen.sceneEnrollUrl`、`qwen.pureTtsUrl`、`qwen.sceneTtsUrl`，并在 `api-server/src/lib/qwen.ts` 中分别用于纯粹版/场景版建声与 TTS。为兼容现有环境，仍保留旧 `QWEN_ENROLL_URL` / `QWEN_TTS_URL` 与 `qwen.enrollUrl` / `qwen.ttsUrl` 作为回退来源；同步更新 `api-server/config.example.yaml`、`README.md`、`DEPLOY.md` 的配置示例与环境变量说明。执行 API `bunx tsc --noEmit`、API `bun run build`、根 `npm run typecheck` 通过。
- 2026-04-26 17:45 +08:00：收紧文本转语音准入规则：移除 `api-server/src/routes/tts.ts` 中“匿名无 active voice 时回退到 `trialVoiceId`”的兜底逻辑，统一要求必须存在已建立且未失效的 active 声纹才能提交 TTS。前端 `components/voice-studio/use-voice-studio-state.ts` 与 `components/voice-studio/tts-panel.tsx` 同步移除匿名免费试用判定，改为根据纯粹版/场景版 active 声纹是否存在决定按钮可用性与提示文案。执行 API `bunx tsc --noEmit`、根 `npm run typecheck`、根 `npm run lint` 通过。
- 2026-04-26 18:28 +08:00：修复首页 6 个 UI/UX 问题：前端将 `response.json()` 直读改为 `readJsonSafely` 并统一把 JSON 解析/HTML 异常收口为用户文案，避免暴露内部报错；登录弹窗补齐 `dialog` 语义、`aria-modal`、Esc 关闭、Tab 焦点圈定与关闭后焦点回收，并移除 `AuthPanel` 双层卡片；TTS 面板改为“场景选择 -> 输入文本”顺序；建声区将主 CTA 提前、移动端步骤提示压缩为三枚短卡并下移；头部新增未登录试用提示条强化 1 次 / 30 字规则。执行根 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-29 14:33 +08:00：依据 `.kilo/plans/1777443391466-kind-panda.md` 实现 TTS 免费次数与使用码准入：新增 `User.freeTtsUsedAt`、`AnonymousUser.freeTtsUsedAt`、`UsageCode`、`TtsAccessKind` 与 `UsageCodeModule`，补齐初始化 SQL、部署兼容回填、数据字典、使用码哈希/生成脚本和 Bun 单测；`POST /api/tts` 在事务内区分免费态与使用码态，匿名用户携带使用码直接拒绝，注册用户使用码创建任务后原子消费。前端新增 `/api/tts/usage` 状态、动态 30/500 字上限、注册用户使用码输入框与匿名登录提示；README/PRODUCT 同步记录规则。验证通过：API/根 Prisma `validate && generate`、`bun test src/lib/usage-code.test.ts`、API `bunx tsc --noEmit`、根 `npm run typecheck`、`npm run lint`、`npm run build`、API `bun run build`；`npm run build` 仍保留既有静态导出 rewrites warning。使用码生成脚本未在验证中实际打印明文码，避免产生无法追溯的运营码输出。
- 2026-04-29 14:45 +08:00：完成本地 QA 复核，未发现阻断问题；根据复核结果补充 README/PRODUCT 说明：同一匿名 Cookie 会话登录后，已使用的匿名免费机会会迁移到注册账号，避免重复领取免费机会。剩余未做项为真实 PostgreSQL 并发压测与浏览器手工交互验证。
- 2026-04-26 18:42 +08:00：按 `polish` 精修首页 TTS 面板的场景选择控件：将原生下拉框重做为暖灰双层触发器，增加独立箭头按钮区、hover / focus 边界反馈与更稳定的触控高度；同时把下拉后的选中信息改为结构化说明区，未选择时明确显示“纯粹版语音”模式，已选择时展示场景标签与 instruction 文案，整体更符合“低干扰任务台”的产品 register。执行根 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-26 19:48 +08:00：依据 `.kilo/plans/1777203586137-curious-moon.md` 为前端工作区新增轻量模块切换条：新增 `WorkspaceModuleNav` 并分别挂载到首页与 `/video-analysis` 页面，新增 `BilibiliAnalysisWorkspace` 作为 B 站视频分析骨架页，首页保持 `WorkspaceHeader`、建声区、TTS 区原结构不重构；同步最小更新 `DESIGN.md` 的 Navigation 章节。执行根项目 `npm run lint`、`npm run typecheck`、`npm run build` 通过，`build` 仍保留项目既有 `output: export` 与 `rewrites` 组合警告。
- 2026-04-26 20:18 +08:00：根据视频分析模块的后端设计讨论，新增 `.kilo/plans/1777205899474-steady-orbit.md` 分阶段实施计划，明确采用 `Postgres + job table + Python poller` 作为首期任务中转方案，并将 `NATS JetStream` 作为后续扩展阶段的可选升级方向；本次仅新增计划文档与日志记录，未执行构建验证。
- 2026-04-26 20:21 +08:00：继续补充视频分析异步链路方案说明：在 `.kilo/plans/1777205899474-steady-orbit.md` 中增加 `Dapr` 评估结论，明确当前阶段不建议为单一 worker 场景提前引入 `Dapr`，仅在后续系统平台化并需要统一 `pub/sub`、状态存储、服务调用与工作流时再评估 `Dapr + NATS` 组合；本次仅更新计划文档与日志记录，未执行构建验证。
- 2026-04-26 20:30 +08:00：完成 `.kilo/plans/1777205899474-steady-orbit.md` 阶段 1 落地：在根与 `api-server` 两套 Prisma schema 中新增 `VideoSource` / `VideoAnalysisJob` 及相关枚举，为视频分析任务补齐数据库索引；新增 `api-server/src/routes/video-analysis.ts`，实现 `POST /api/video-analysis/jobs`、`GET /api/video-analysis/jobs/:jobId`、`GET /api/video-analysis/jobs`、`GET /api/video-analysis/workspace` 四个接口骨架，要求登录后使用，并通过 DTO 组装响应而非直出 ORM 记录；同时在 `api-server/src/lib/validation.ts` 增加视频分析创建参数校验，并在 `api-server/src/index.ts` 挂载新路由。执行 API / 根 `bunx prisma generate`、API `bunx tsc --noEmit`、API `bun run build`、根 `npm run typecheck` 通过。
- 2026-04-27 11:24 +08:00：按 `.kilo/plans/1777203727348-playful-eagle.md` 落地阶段 2 最小闭环：新增独立 `video-analysis-worker/` Python 子工程，使用 `psycopg + requests + pydantic` 实现任务抢占、B 站元信息抓取、字幕优先/ASR 兜底、LLM 结构化结果校验与 `READY/FAILED` 回写；同步补充 `.env.example`、`.gitignore`、`Makefile`、`README.md` 与 worker 运行说明，保持现有 4 个 API 协议不变。
- 2026-04-27 12:54 +08:00：分析 `Nemo2011/bilibili-api` 的视频音频、字幕、Cookie 与二维码登录机制后，在 `video-analysis-worker/lib/biliapi` 新增基于 Patchright 的精简 B 站客户端，保留视频信息、字幕列表、DASH 音频地址与二维码登录，明确不包含弹幕和 WebSocket；`services/bilibili.py` 改为调用该本地客户端，并补充 Patchright 依赖和 README 说明。执行 `python3 -m compileall video-analysis-worker/lib video-analysis-worker/services/bilibili.py` 与 `lib.biliapi` 导入验证通过；当前环境未安装 `patchright`，运行真实抓取前需执行依赖安装与 `patchright install chromium`。
- 2026-04-27 14:21 +08:00：为 Patchright 版 `biliapi` 增加有头登录测试能力：`PatchrightClient` 新增打开 B 站登录页、检测/等待 `SESSDATA` 登录态的方法；新增 `video-analysis-worker/scripts/test_biliapi_subtitle_download.py`，可用有头浏览器扫码或网页登录后，按指定 BV 获取 `cid`、读取 `x/player/v2` 字幕轨道并下载字幕 JSON 与纯文本；README 补充测试命令。执行 `python3 -m compileall video-analysis-worker/lib video-analysis-worker/scripts/test_biliapi_subtitle_download.py` 与脚本导入验证通过。
- 2026-04-29 12:44 +08:00：建声前端恢复“录音 + 文件上传”双入口；上传端新增 `mp3 / wav / w4v` 扩展名与 MIME 联合校验，并在服务端补充按文件名回退识别 `w4v` / `video/mp4`，同时将前端用户提示中的“存入 MinIO”统一收敛为“先保存”。执行根 `npm run typecheck` 验证。
- 2026-04-29 13:30 +08:00：按录音交互反馈将建声按钮从“按住开始、松开结束”改为“点击开始、再次点击结束”；前端移除 mouse/touch 长按状态机，统一收敛为单一 `onClick` 切换逻辑，并补充 `recording start requested / started / stop requested / button clicked` 最小诊断日志，便于区分授权弹窗、启动中与真实录音中的状态切换；同步更新录音区、步骤提示与头部说明文案。
- 2026-04-29 13:36 +08:00：将建声区“开始录音”与“音频上传”两个独立入口合并为同一组左右分体按钮：左侧保留录音主操作，右侧保留文件上传触发，沿用既有上传/建声/删除中的禁用约束与文件选择逻辑，不改后端接口与状态流；执行根 `npm run typecheck` 验证。
- 2026-04-29 13:38 +08:00：清理上传控件 `accept` 重复项，新增前端共享方法按受支持扩展名生成去重后的文件选择值，仅向系统文件选择器暴露 `.wav,.mp3,.w4v`，保留现有运行时 MIME/后缀校验逻辑不变；执行根 `npm run typecheck` 验证。
- 2026-04-29 13:43 +08:00：为前端录音流程新增 30 秒自动结束机制：新增 `MAX_RECORD_SECONDS = 30` 常量，在录音开始后按剩余时长注册自动停止定时器，超时后自动结束并进入上传；同步更新建声区状态文案、帮助文案与按钮标签，明确提示“最长 30 秒自动结束”；执行根 `npm run typecheck` 验证。
- 2026-04-29 13:46 +08:00：修复 TTS 下载文件名不可读问题：`/api/tts/:jobId/download` 改为根据实际 `outputContentType` 推导扩展名，并在 `Content-Disposition` 中返回 `tts-pure|scene-<jobId>.<ext>` 文件名，避免浏览器“另存为”时只显示任务 ID 或无后缀乱名；执行 API `bunx tsc --noEmit` 验证。
- 2026-04-29 13:49 +08:00：继续调整 TTS 下载文件名，将 `Content-Disposition` 中的 `<jobId>` 替换为任务 `createdAt` 的毫秒级时间戳，生成 `tts-pure|scene-<timestamp>.<ext>`，减少暴露内部任务 ID 并提升文件名可读性；执行 API `bunx tsc --noEmit` 验证。
- 2026-04-29 14:52 +08:00：按首页 UI 反馈修正文案与布局：未登录提示移除“无限生成”表述，改为登录后继续建声并通过使用码生成；建声区三步提示上移并合并为一个完整说明块；TTS 最新结果与历史语音下载按钮统一靠右。执行根 `npm run typecheck`、`npm run lint` 通过。
- 2026-04-29 15:12 +08:00：实现注册用户通用使用码链路：新增 `GENERAL_USAGE_CODE` 权益来源与 `TtsJob.usageCodeValue` 快照字段，默认通用码为 `123456` 并支持 `USAGE_CODE_GENERAL_CODE` / `usageCode.generalCode` 配置修改；TTS 创建时区分免费生成、通用使用码生成、非通用一次性使用码生成，并记录每条语音的使用码来源。前端在文本输入前展示通用使用码、用法说明与输入框，历史语音和最新结果显示来源标注。执行根/API Prisma validate、generate、API `bun test src/lib/usage-code.test.ts`、API `bunx tsc --noEmit`、根 `npm run typecheck`、根 `npm run lint`、根 `npm run build`、API `bun run build`、API `bunx prisma db push`、`bash -n deploy.sh` 通过；前端 build 仍保留既有静态导出 rewrites warning。
- 2026-04-29 19:49 +08:00：依据 `react-ts-audio-recorder` 文档将前端建声录音链路从“原生 `MediaRecorder` 录制后本地转 WAV”切换为 `MultiRecorder({ format: "wav" })` 直接输出 WAV；`useVoiceStudioState` 移除 `MediaRecorder` / chunk / 转码逻辑，停止录音后直接生成 `enrollment.wav` 并复用现有上传接口；删除前端 `pickRecordingMimeType` 与 `convertBlobToWavFile` 旧逻辑，并新增 `public/pcm-worklet.js` 供 WAV 模式 `AudioWorklet` 使用。执行根 `npm run typecheck`、`npm run lint`、`npm run build` 通过；`build` 仍保留项目既有静态导出 rewrites warning，`npm install` 过程中仍出现既有 Prisma Node engine warning。
- 2026-04-29 20:02 +08:00：调整 TTS 登录态权益交互：登录用户始终显示使用码输入区；若仍有免费机会，则在“使用码”标题后展示默认勾选的“试用（最多 30 个字）”复选框，并让 TTS 文本上限与提交体按“试用 / 使用码”切换；同时将使用码提示补充为“试用单次最多 30 个字”。执行根 `npm run typecheck`、`npm run lint` 验证。
- 2026-04-29 20:06 +08:00：调整建声录音区布局：将“当前录音素材”面板从提示区上方移动到“开始录音 / 音频上传”下方，并把列表显示顺序改为旧录音在上、最新录音在最下面；同时将录音素材默认选中与删除后的回退项同步改为列表最后一条，保持与新视觉顺序一致。执行根 `npm run typecheck`、`npm run lint` 验证。
- 2026-04-29 20:07 +08:00：按测试需求人工校正免费试用状态：连接本地 PostgreSQL `voice_mvp` 数据库，将 `User.freeTtsUsedAt` 置空并立即回读确认；回读结果显示该账号当前 `freeTtsUsedAt` 已为空，可重新走登录态试用分支进行前端验证。
- 2026-04-29 20:12 +08:00：补充 TTS 登录态权益提示：在“使用码 / 试用”区域新增文案“勾选试用后，不用输入使用码”，仅在仍有试用机会时显示，减少试用与使用码并存时的理解歧义。执行根 `npm run typecheck`、`npm run lint` 验证。
- 2026-04-29 21:52 +08:00：依据 `.kilo/plans/1777466247689-lucky-tiger.md` 实现第一版 admin 前端与配套接口收口：新增 `app/admin/*` 路由树、`components/admin/*` 深色控制台组件、`lib/admin-api.ts` 客户端请求封装与当前标签页级 Basic Auth 凭证暂存；页面覆盖概览、用户、邀请码、语音生成四个视图，支持客户端拉取 `/api/admin/*`、URL 驱动筛选/分页、用户与任务详情面板、邀请码批量生成结果内联展示。后端同步补齐 admin 查询参数与统计口径：邀请码列表增加创建时间范围筛选，语音生成列表增加 `status` 筛选，渠道统计增加 `voiceGenerations` 聚合。执行验证通过：根 `npm run typecheck`、`npm run lint`、`npm run build`、API `bunx tsc --noEmit`；前端 `build` 仍保留项目既有 `output: export` 与 `rewrites` 组合 warning，但静态导出已成功产出 `/admin`、`/admin/users`、`/admin/invite-codes`、`/admin/voice-generations` 页面。
- 2026-04-29 21:23 +08:00：依据 `.kilo/plans/1777466247689-lucky-tiger.md` 完成运营后台第一版后端实现：同步扩展根目录与 `api-server/` 两份 Prisma schema，新增 `AnalyticsVisitor` / `AnalyticsSession` / `AnalyticsEvent` 与 `AnalyticsEventName` / `AnalyticsChannel`；在 `api-server` 新增 `/api/analytics/collect`、`/api/admin/analytics/*`、`/api/admin/users/*`、`/api/admin/invite-codes/*`、`/api/admin/voice-generations/*` 路由，以及 analytics/admin service、渠道判定、Basic Auth 中间件、分页/时间范围 schema。为什么这么做：当前仓库只有面向“当前用户/匿名会话”的 owner-scoped 接口，缺少跨用户运营查询视角，因此首版按计划退化为“完整后端 API + 中文说明”，不额外硬搭后台前端。同步更新 `api-server/init-db.sql`、`api-server/.env.example`、`api-server/config.example.yaml`、`api-server/DATABASE_DICTIONARY.md`、`README.md`、`DEPLOY.md` 与根 `.env.example`，并修正文档中关于 `UsageCode` 明文存储、后台 Basic Auth 与 analytics `user_id` 信任边界的漂移。验证结果：根/API `bunx prisma validate` 通过，根/API `bunx prisma generate` 通过，API `bunx tsc --noEmit` 通过，API `bunx prisma db push` 已成功同步本地 PostgreSQL，API `bun run build` 通过并产出新的单文件二进制。已知限制：本轮仍未实现后台前端页面；`/api/admin/voice-generations/:id` 详情不直接提供管理员下载链接，避免返回一个仍受 owner-scoped 鉴权保护的不可用地址；analytics 仍是轻量运营埋点，不是通用事件平台。
